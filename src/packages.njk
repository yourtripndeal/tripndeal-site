---
title: Travel Packages
layout: layout.njk
---

<h1>Available Packages</h1>

<div class="packages-grid">
  {% for pkg in collections.packages %}
    <div class="package-card">
      <h2><a href="{{ pkg.url }}">{{ pkg.data.title }}</a></h2>
      {% if pkg.data.excerpt %}
        <p>{{ pkg.data.excerpt }}</p>
      {% endif %}
      <a class="btn" href="{{ pkg.url }}">View Details</a>
    </div>
  {% endfor %}
</div>
