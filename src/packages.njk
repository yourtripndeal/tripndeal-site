---
layout: layout.njk
title: Packages
permalink: "/packages/index.html"
---

<h2>Featured Packages</h2>
<div class="grid">
{% for pkg in collections.packages | reverse %}
  <a class="card-link" href="{{ pkg.url }}">
    <article class="card">
      <p class="eyebrow">{{ pkg.data.destination }}{% if pkg.data.duration %} • {{ pkg.data.duration }}{% endif %}</p>
      <h3>{{ pkg.data.title }}</h3>
      {% if pkg.data.image %}
      <img src="{{ pkg.data.image | url }}" alt="{{ pkg.data.title }}"/>
      {% endif %}
      {% if pkg.data.price %}<p class="price">From {{ pkg.data.price }}</p>{% endif %}
      <p class="more">View details →</p>
    </article>
  </a>
{% endfor %}
</div>